import React, { FC } from 'react';
import { BaseWrapper } from 'modules/BaseWrapper';
import { Workspace } from 'modules/Workspace';

const App: FC = () => (
  <BaseWrapper>
    <Workspace />
  </BaseWrapper>
);

export default App;
